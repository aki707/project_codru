const express = require("express");
const Course = require("../models/courseSchema");
const router = express.Router();

route.post("/add-courses", async(req,res)=>{
    try {
        
    } catch (error) {
        
    }
})

module.exports = router;